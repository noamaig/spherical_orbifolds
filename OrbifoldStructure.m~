classdef OrbifoldStructure < handle
    %the object that generates the entire structure of the orbifold, gets
    %the mesh & the cones, and outputs the disk mesh of the basic tile and
    %the 
    
    properties
    end
    
    
    methods (Abstract)
        
        [boundary,cutmesh]=generate(obj);
    end
end

